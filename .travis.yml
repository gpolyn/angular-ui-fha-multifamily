language: node_js
node_js:
  - '6'
script: 
  - gulp test
  - gulp
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: dist
  repo: gpolyn/angular-fha
  target_branch: master
  on:
    branch: dev
